project: "SP-swap-order"
program: sweep_for_hyperparameters.py
method: bayes
metric:
  name: mean_reward_test
  goal: maximize
parameters:
  exploration_rate:
    min: 0.1
    max: 1.0
  learning_rate:
    min: 0.01
    max: 1.0
  discount_factor:
    min: 0.01
    max: 1.0